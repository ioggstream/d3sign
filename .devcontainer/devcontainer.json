{
  "name": "d3sign",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-24-bookworm",
  "customizations": {
    "vscode": {
      "extensions": [
        "anthropic.claude-code"
      ]
    }
  },
  // Kept commented out: postCreateCommand can fail silently in some environments
  // (e.g. no network access), which would block container creation.
  // Run manually if needed: corepack enable && corepack prepare pnpm@9.15.4 --activate
  // "postCreateCommand": "corepack enable --install-directory . && node --version && pnpm --version"
}
